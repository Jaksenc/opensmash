steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file=pipeline/web-prototype/docker/worker.Dockerfile
      - --tag=${_IMAGE}
      - .
images:
  - ${_IMAGE}
options:
  machineType: E2_HIGHCPU_8
  diskSizeGb: 100
