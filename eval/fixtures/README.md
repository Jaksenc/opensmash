# Evaluation fixtures

- `replays/` contains deterministic BattleShip input tours and their metadata.
- `frames/` contains captured joint-frame data and the source frame dump used
  by the offline render and report tools.

Generated screenshots, ratings, and evaluation cells remain elsewhere under
`eval/` and are ignored where appropriate.
